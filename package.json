{
  "name": "api-platform-admin-on-rest-gen",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "admin-on-rest": "^1.3",
    "react-scripts": "1.0.14"
  },
  "dependencies": {
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
